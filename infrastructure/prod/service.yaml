apiVersion: v1
kind: Service
metadata:
  name: open-swe
  namespace: open-swe-prod
  labels:
    app.kubernetes.io/name: open-swe
    app.kubernetes.io/component: controller
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: open-swe
    app.kubernetes.io/component: controller
  ports:
    - name: http
      port: 80
      targetPort: http
